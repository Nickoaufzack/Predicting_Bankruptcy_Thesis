\chapter{Experimental Results}
\label{ch:results}

% EQUATOR: ~2 pages. Model scores + FI results; the three-way DIVERGENCE at the center.
%   Strictly predictive relevance -- NO causal claims. Plots embedded (mind the <=20 page budget).
% Use EXACT numbers from Final_Code_Output, never from memory:
%   GB F2 ~0.561 (best, max_depth=1 stump), RF ~0.546/0.550, LR ~0.483.
% Divergence: LR -> Debt Ratio #1 (all 3 methods agree); RF/GB native Gini -> Persistent EPS #1
%   (Gini artifact); GB SHAP -> Borrowing Dependency #1 (0.356, corrects Gini); Permutation on
%   RF/GB -> characteristically tiny values (correlation masking).

