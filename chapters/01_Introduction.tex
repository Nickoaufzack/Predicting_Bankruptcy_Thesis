\chapter{Introduction}
\label{ch:introduction}

% EQUATOR: ~1 page. GUIDE (Farbmacher) -- the introduction must:
%   (1) motivate the topic (why interesting, current reference, larger context);
%   (2) specify the research question (what is analysed, what is left out);
%   (3) briefly summarize the literature (with journal-quality awareness -> see ch. Literature);
%   (4) give an outlook on the most important results + explain the structure of the main part.
% Positioning: the "Clean Pipeline" -- real historical data only, no synthetic (SMOTE) firms;
%   extends Wang & Liu (2021) by adding the feature-importance / interpretability layer.

